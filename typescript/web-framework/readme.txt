Framework
- fetch some data
- attempt to render some content on the screen
- handle user events

setup evironments 
> npm init
> npm install -g parcel-bundler
> parcel index.html
> npm install -g json-server // adding JSON server
> npm install axios

commands
> parcel index.html
> json-server -w db.json
> npm run start:parcel // after add the command line into the package.json

> tsc --init // checkin the tsconfig.json for restrict option
